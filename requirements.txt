bleak
aiomqtt
# Used directly (not just via bleak's bluezdbus backend) for the bluetooth
# stack auto-recovery escalation in mower_mqtt.py.
dbus-fast

# Patched fork with fixes for GetMessage / GetOverride / GetTask parsing
# bugs, pending upstream review (alistair23/AutoMower-BLE PRs #151, #152,
# #153), plus AuthenticationFailed pairing diagnostics and SILENO sense
# model entries ported from upstream PRs #158 and #159. Switch this back
# to the plain `automower-ble` PyPI package once those are merged.
git+https://github.com/gruijter/AutoMower-BLE.git@combined-fixes
